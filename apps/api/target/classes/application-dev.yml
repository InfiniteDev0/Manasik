management:
  endpoint:
    health:
      # Full health detail locally — handy while wiring up Postgres and Redis
      # in Phase 3. Production keeps this at `when-authorized`.
      show-details: always

springdoc:
  swagger-ui:
    enabled: true

logging:
  level:
    com.manasik.api: DEBUG
    org.springframework.security: DEBUG
