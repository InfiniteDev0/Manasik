{
  "name": "@manasik/api",
  "version": "0.1.0",
  "private": true,
  "description": "Spring Boot API. This package.json is a shim so Turborepo can drive Maven — there is no Node code here.",
  "scripts": {
    "dev": "mvn spring-boot:run",
    "build": "mvn -B clean package -DskipTests",
    "test": "mvn -B test",
    "verify": "mvn -B verify",
    "clean": "mvn -B clean",
    "typecheck": "echo \"no-op: apps/api is Java, compilation is covered by 'build'\""
  }
}
