// PLACEHOLDER — not built yet. Exists so the sidebar link resolves instead of
// 404-ing, which keeps the shell navigable while sections are filled in.
export default function GroupsPage() {
  return (
    <div>
      <h1 className="font-heading text-2xl font-semibold tracking-tight">Groups</h1>
      <p className="mt-1 text-sm text-muted-foreground">Coming soon.</p>
    </div>
  );
}
