// PLACEHOLDER — the upgrade CTA in the header links here. Real pricing tiers
// and Stripe checkout are a later phase; this exists so the link resolves.
export default function PricingPage() {
  return (
    <div>
      <h1 className="font-heading text-2xl font-semibold tracking-tight">Plans</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Pricing and billing are not set up yet.
      </p>
    </div>
  );
}
