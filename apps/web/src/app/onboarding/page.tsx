// PLACEHOLDER — ROADMAP Phase 7 builds the real create-workspace wizard
// (Step 1 agency details → Step 2 agency profile → Step 3 invite team, skippable).
//
// This exists only so the route is a valid module. Next's route validator
// rejects an empty page.tsx, which fails `pnpm typecheck` for the whole app.
export default function OnboardingPage() {
  return null;
}
