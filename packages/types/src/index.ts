// Empty while the dashboard is rebuilt from scratch.
//
// Once the Supabase tables exist, generate the database types into this
// package (`supabase gen types typescript`) so a column rename becomes a
// compile error instead of a runtime bug.
export {};
