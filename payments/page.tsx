// PLACEHOLDER — not built yet. Exists so the sidebar link resolves instead of
// 404-ing, which makes the shell navigable while sections are filled in.
export default function PaymentsPage() {
  return (
    <div>
      <h1 className="font-heading text-2xl font-semibold tracking-tight">Payments</h1>
      <p className="mt-1 text-sm text-muted-foreground">Coming soon.</p>
    </div>
  );
}
